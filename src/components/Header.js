import React, { Component } from "react";
import "../styles/Header.css"

class Header extends Component {

    render() {
        return (
            <header className="header">
                <p className="header--title">CV Generator</p>
            </header>
        )
    }
};

export default Header;